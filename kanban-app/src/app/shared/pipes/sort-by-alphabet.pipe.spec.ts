import { SortByAlphabetPipe } from './sort-by-alphabet.pipe';

describe('SortByAlphabetPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByAlphabetPipe();
    expect(pipe).toBeTruthy();
  });
});
