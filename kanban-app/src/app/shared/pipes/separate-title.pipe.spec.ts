import { SeparateTitlePipe } from './separate-title.pipe';

describe('SeparateTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
