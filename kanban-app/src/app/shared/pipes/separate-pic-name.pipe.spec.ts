import { SeparatePicNamePipe } from './separate-pic-name.pipe';

describe('SeparatePicNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SeparatePicNamePipe();
    expect(pipe).toBeTruthy();
  });
});
